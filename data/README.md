Data is NOT in git. Setup:
1. Get images.zip + labels.csv from https://github.com/AlexOlsen/DeepWeeds
2. unzip images.zip -d data/images
3. cp labels.csv data/labels.csv
Expect 17509 files in data/images/
